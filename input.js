"use strict";
var userInput; // accepts unknown value enterd by the user
userInput = 5;
var UserName;
userInput = "max";
// known type for unknown 
if (typeof userInput === 'string') {
    UserName = userInput;
}
